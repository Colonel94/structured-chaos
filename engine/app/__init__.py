"""Adaptive Intake headless engine.

`ingest -> structured case + confidence + provenance` is an API; the UI is its first
client, not its container (CLAUDE.md §4). Modules mirror the pipeline stages.
"""
