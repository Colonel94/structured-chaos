"""Cloud backend implementations (Cohere ASR, Claude LLM, BGE-M3 embeddings, MinIO/S3 blob).

Built in Phase 2. Referenced lazily by registry.py so Phase-0 (fake backend) imports
cleanly without these modules existing yet.
"""
